import React from "react";
import { User, ArtistWithUsers, VenueWithUsers } from "types";
// Firebase
import { User as FirebaseUser } from "firebase/auth";

export default React.createContext({
  // setters
});
